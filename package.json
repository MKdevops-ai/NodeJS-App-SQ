{
  "name": "node-app",
  "description": "Nodeapp for CICD test",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "express": "4.17.1"
  },
  "devDependencies": {
    "mocha": "9.1.1",          // Testing framework
    "supertest": "6.1.6",      // API testing tool
    "nyc": "15.1.0"            // Code coverage tool
  },
  "scripts": {
    "start": "node index.js",  // Start the application (typically used in local/production environments)
    "test": "nyc --reporter=lcov --reporter=text mocha ./test/test.js"  // Run tests and generate coverage report with nyc
  }
}
