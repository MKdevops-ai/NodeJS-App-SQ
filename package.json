{
  "name": "node-app",
  "description": "Nodeapp for CICD test",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "express": "4.17.1"
  },
  "devDependencies": {
    "mocha": "9.1.1",
    "supertest": "6.1.6",
    "nyc": "15.1.0"  // Add nyc for code coverage
  },
  "scripts": {
    "start": "node index.js",
    "test": "nyc --reporter=lcov --reporter=text mocha ./test/test.js"  // Run tests with coverage
  }
}

